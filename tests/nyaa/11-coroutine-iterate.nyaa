var map = {1, 2, 3, 4, 5, 6, 7, 8}

var t = new coroutine(lambda() {
	for k, v in pairs(map) {
		yield(k, v)
	}
})

log(t:resume())
log(t:resume())
log(t:resume())
log(t:resume())
log(t:resume())
log(t:resume())
log(t:resume())
log(t:resume())
